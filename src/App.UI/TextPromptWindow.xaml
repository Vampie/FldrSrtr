<!-- No ui:WindowHelper.UseModernWindowStyle — see ConflictDialog.xaml for why. -->
<Window x:Class="FldrSrtr.TextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FldrSrtr"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        Title="FldrSrtr" Height="150" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,8" />
        <TextBox x:Name="ValueTextBox" Grid.Row="1" KeyDown="ValueTextBox_KeyDown" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="{local:Loc Common.Cancel}" Width="90" Margin="0,0,8,0" Click="Cancel_Click" IsCancel="True" />
            <Button Content="{local:Loc Common.Ok}" Width="90" Click="Ok_Click" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
