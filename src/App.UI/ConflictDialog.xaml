<!-- Deliberately no ui:WindowHelper.UseModernWindowStyle here: on this Windows build it makes an
     owned dialog window render as a solid accent-colored rectangle (no content at all) — only
     MainWindow (unowned) renders correctly with it. Fluent control styling still applies via the
     globally merged ThemeResources/XamlControlsResources in App.xaml regardless of window chrome. -->
<Window x:Class="FldrSrtr.ConflictDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FldrSrtr"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        Title="{local:Loc ConflictDialog.Title}" Height="180" Width="520"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <StackPanel Margin="16">
        <TextBlock x:Name="MessageText" TextWrapping="Wrap" Margin="0,0,0,16" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{local:Loc Common.Skip}" Tag="Icons/icon-skip.png" Style="{StaticResource IconButton}" Width="90" Margin="0,0,8,0" Click="Skip_Click" />
            <Button Content="{local:Loc Common.Overwrite}" Tag="Icons/icon-overwrite.png" Style="{StaticResource IconButton}" Width="90" Margin="0,0,8,0" Click="Overwrite_Click" />
            <Button Content="{local:Loc Common.Rename}" Tag="Icons/icon-edit.png" Style="{StaticResource IconButton}" Width="90" Click="Rename_Click" IsDefault="True" />
        </StackPanel>
    </StackPanel>
</Window>
